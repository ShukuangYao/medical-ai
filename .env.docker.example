# Compose（云服务器）推荐配置：全部用 docker compose 里的服务名互联
#
# 用法：
#   cd medical-ai
#   cp .env.docker.example .env
#   # 填好密钥/密码后：
#   docker compose up -d
#
# 注意：容器内不要用 127.0.0.1 连接其他依赖，127.0.0.1 指向“当前容器自己”。

# LLM（RAG / 普通问答常用：DashScope OpenAI 兼容接口）
DASHSCOPE_API_KEY=your_dashscope_api_key_here
LLM_MODEL=qwen3.5-flash
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# 病历分析（多 Agent）默认走 DeepSeek。仅跑 RAG 可保持注释；需要病历分析时再取消注释并填写（与 RAG 共用 python-service）：
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# DEEPSEEK_API_BASE=https://api.deepseek.com

# Agent：请求未带 model_provider 时的默认提供方（deepseek | qwen，默认 deepseek）
# AGENT_DEFAULT_LLM_PROVIDER=deepseek

# Agent：按角色覆盖模型 id（可选）。变量名示例见 docs/design.md；SSE error / phase 契约见 docs/design.md

# LangSmith（Tracing / 可观测性）
# Node 与 Python 容器均读取；工具调用在 LangSmith 中为独立子 span（tool:{工具名}），便于对 Phase 1 检索/重排/图谱等步骤单独看耗时与失败原因。
LANGSMITH_TRACING_V2=true
LANGSMITH_API_KEY=ls__your_langsmith_api_key_here
LANGSMITH_PROJECT=medical-ai

# Milvus向量数据库配置（compose service: milvus-standalone）
MILVUS_HOST=milvus-standalone
MILVUS_PORT=19530
MILVUS_COLLECTION=medical_knowledge

# ElasticSearch配置（compose service: elasticsearch）
ES_HOST=elasticsearch
ES_PORT=9200
ES_INDEX=medical_knowledge

# 嵌入/重排序模型配置
EMBEDDING_MODEL=BAAI/bge-large-zh
RERANKER_MODEL=BAAI/bge-reranker-large

# Neo4j配置（compose service: neo4j）
NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=medical_demo_2025
NEO4J_DATABASE=neo4j

# Redis配置（compose service: redis）
REDIS_URL=redis://redis:6379

# Python服务配置（Node 容器访问 Python 容器用 service 名）
PYTHON_SERVICE_URL=http://python-service:8000

# Node服务配置
NODE_PORT=3001
